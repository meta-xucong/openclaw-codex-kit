---
name: healthcheck
description: 检查 Windows 主机的网络暴露、更新、备份、磁盘保护和远程访问风险。
---

# 主机安全检查状态

本技能在当前能力包中标记为 `unsupported/disabled`。它原本依赖外部运行时的专用诊断命令，
而 Codex 安装包没有内置那套运行时；继续保留命令示例会造成误导，因此不提供自动执行入口。

## 可用替代

如果用户明确要求 Windows 安全检查，应在确认目标主机和权限后，使用系统自带的只读工具逐项检查：

- Windows 安全中心、Windows Update、防火墙和 BitLocker 状态
- 当前监听端口、远程桌面和 SSH 暴露面
- 备份可恢复性、磁盘空间和关键软件版本

任何修改防火墙、远程访问、启动项、注册表或磁盘配置的操作，都必须先说明风险并取得明确确认。
